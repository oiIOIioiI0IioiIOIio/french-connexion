# Requirements pour french-connexion
# Mis à jour avec toutes les dépendances nécessaires
# ========================================
# SDK Mistral AI
# ========================================
mistralai>=1.0.0
# ========================================
# Parsing et manipulation de fichiers
# ========================================
PyYAML>=6.0.1
python-frontmatter>=1.0.0  # Pour parser les fichiers Markdown avec frontmatter
python-dotenv>=1.0.0
# ========================================
# Utilitaires
# ========================================
colorlog>=6.7.0  # Logs colorés (optionnel mais recommandé)
feedparser>=6.0.10  # Pour parser les flux RSS
wikipedia         # Nécessaire pour le script d'enrichissement Wikipédia
# ========================================
# Pour les tests (optionnel)
# ========================================
pytest>=7.4.0
pytest-cov>=4.1.0
# ========================================
# Notes importantes
# ========================================
# 1. Pour installer : pip install -r requirements.txt
# 2. Pour mettre à jour : pip install --upgrade -r requirements.txt
# 3. Pour GitHub Actions, ce fichier sera utilisé automatiquement
# 4. Variables d'environnement requises :
#    - MISTRAL_API_KEY (obligatoire)
#    - MISTRAL_MODEL (optionnel, défaut: open-mistral-nemo)
