# Requirements pour french-connexion
# Mis a jour avec toutes les dependances necessaires
# ========================================
# SDK Mistral AI (optionnel - ameliore la qualite quand disponible)
# ========================================
mistralai>=1.0.0
# ========================================
# Parsing et manipulation de fichiers
# ========================================
PyYAML>=6.0.1
python-frontmatter>=1.0.0  # Pour parser les fichiers Markdown avec frontmatter
python-dotenv>=1.0.0
# ========================================
# NLP (Traitement du langage naturel)
# ========================================
spacy>=3.7.0  # NER pour la generation automatique de liens et extraction de relations
# ========================================
# Utilitaires
# ========================================
colorlog>=6.7.0  # Logs colores (optionnel mais recommande)
wikipedia         # Necessaire pour l'enrichissement Wikipedia
# ========================================
# Pour les tests (optionnel)
# ========================================
pytest>=7.4.0
pytest-cov>=4.1.0
# ========================================
# Notes importantes
# ========================================
# 1. Pour installer : pip install -r requirements.txt
# 2. Pour le modele Spacy : python -m spacy download fr_core_news_lg
# 3. Pour GitHub Actions, ce fichier sera utilise automatiquement
# 4. Variables d'environnement optionnelles :
#    - MISTRAL_API_KEY (optionnel, ameliore la qualite de l'extraction)
#    - MISTRAL_MODEL (optionnel, defaut: open-mistral-nemo)
